services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: tickets_pog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d tickets_pog"]
      interval: 1s
      timeout: 5s
      retries: 20
